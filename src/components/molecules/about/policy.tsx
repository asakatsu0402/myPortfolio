// Modules
import React from "react"
// Config
import { policyText } from "../../../config/aboutConfig"

export const Policy: React.VFC = () => {
  // *************** JSX *************** //
  return (
    <div>
      <p>{policyText.ja}</p>
    </div>
  )
}